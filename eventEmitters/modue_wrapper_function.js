(function(exports, require, module, __fileName, __dirname ) {
    /**
     * Each and every module wrapped with the module wrapper function, so the exports, module, require
     * are not the global objects like console.
     *  
     * */ 
    console.log(exports, module, require)
})